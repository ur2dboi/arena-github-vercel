# Netlify / Cloudflare Pages redirects
# Nicer admin URL: your-site.com/admin  ->  admin.html
# (Vercel ignores this file; vercel.json "cleanUrls" does the same there)

/admin      /admin.html     200
/login      /login.html     200
